// Auto-generated by firebolt-gen — do not edit manually.
// Module: Metrics

/** Classification of error type. */
type ErrorType = "network" | "playback" | "entitlement" | "parse" | "aborted" | "unknown";

declare namespace Metrics {
  /** Signals that the app is ready to display content and accept user input. */
  function ready(): Promise<void>;

  /** Signals the app is starting to show video or audio content. */
  function startContent(): Promise<void>;

  /** Signals the app has stopped showing video or audio content. */
  function stopContent(): Promise<void>;

  /** Signals that the app has transitioned to a new page. The pageName parameter identifies the page. */
  function page(pageName: string): Promise<void>;

  /** Logs an error that occurred within the app. */
  function error(errorType: ErrorType, errorMessage?: string): Promise<void>;

  /** Signals that the app has started loading media content. */
  function mediaLoadStart(): Promise<void>;

  /** Signals that media playback has begun. */
  function mediaPlay(): Promise<void>;

  /** Signals that media is actively playing (distinguishes from play intent, e.g., after buffering). */
  function mediaPlaying(): Promise<void>;

  /** Signals that media playback has been paused. */
  function mediaPause(): Promise<void>;

  /** Signals that media playback is waiting (buffering) for data. */
  function mediaWaiting(): Promise<void>;

  /** Signals that the user is seeking to a new playback position. */
  function mediaSeeking(): Promise<void>;

  /** Signals that the user has completed seeking to a new position. */
  function mediaSeeked(): Promise<void>;

  /** Signals that media playback rate has changed (e.g., speed up, slow down). */
  function mediaRateChanged(): Promise<void>;

  /** Signals that media rendition (quality) has changed. */
  function mediaRenditionChanged(): Promise<void>;

  /** Signals that media playback has ended. */
  function mediaEnded(): Promise<void>;

  /** Generic event telemetry allowing apps to log custom metrics and analytics events. */
  function event(eventName: string, eventData?: string): Promise<void>;

  /** Sends app-specific metrics to the platform. Params include agePolicy indicating content maturity if applicable. */
  function appInfo(agePolicy?: Shared.AgePolicy): Promise<void>;

}
